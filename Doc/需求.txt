1、添加DetachDialog()函数，可以释放AttachDialog()的操作重新从xml文件加载皮肤					----------以移除，重新加载一个皮肤，界面库可以自动实现卸载之前的所有控件
2、添加RichEdit支持图片的Ole插件，并且支持拖拽操作
3、添加无窗口文本框、首先考虑创建输入光标
   a、当控件获取焦点的时候，使用BMP创建光标，并设置光标显示位置，显示光标
   b、当控件失去焦点的时候，销毁光标
   c、通过逻辑计算求出光标位置
4、添加异性按钮，主要通过按钮长宽来将将图形转换为几何图像，确定几何图像的圆点，重载基类PtInRect函数，
   a、通过重载这个函数将鼠标点击位置与几何图像的图像函数求交点
   b、如果该点在几何图像内或者是在几何图像上既可以认为点中了该按钮